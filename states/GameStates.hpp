// #include "../GameObjectCom/GameObjectList.hpp"
// #include "../Game.cpp"
#pragma once


class GameStates : public GameObjectList{
public:
    explicit GameStates(float,float);
             ~GameStates();
    // virtual void update(float deltaTime);
    // virtual void reset(float deltaTime);
    // virtual void handleInput(float deltaTime);
};